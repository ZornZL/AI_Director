interface:
  display_name: "编导训练归档"
  short_description: "按用户确认执行入库、去重合并并更新导演能力地图"
  default_prompt: "使用 $archive-director-learning 按我确认的 A/B/C/D 或合并方案归档，并避免创建重复结构。"

policy:
  allow_implicit_invocation: true
